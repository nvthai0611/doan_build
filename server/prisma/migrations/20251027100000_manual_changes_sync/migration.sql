-- AlterTable (already applied manually to database)
-- ALTER TABLE "class_sessions" ADD COLUMN IF NOT EXISTS "cancellation_reason" TEXT;

-- AlterTable (already applied manually to database)
-- ALTER TABLE "teacher_feedbacks" ALTER COLUMN "status" SET DEFAULT 'approved';

-- Note: These changes are already in the database, so this migration file is commented out
-- to avoid re-applying them. We're just marking them in migration history.

