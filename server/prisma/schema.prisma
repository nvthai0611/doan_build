//
// Autogenerated by `prisma-import`
// Any modifications will be overwritten on subsequent runs.
//

//
// _base.prisma
//

// _base.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//
// check.prisma
//

//
// courses.prisma
//

// post.prisma

model Courses {
  id        Int      @id @default(autoincrement())
  title     String   @db.VarChar(255)
  content   String   @db.VarChar()
  author    User?    @relation(fields: [authorId], references: [id])
  authorId  Int?
  create_at DateTime
  update_at DateTime

  @@map("courses") // chỉ định tạo ra bảng "courses"
}

//
// order.prisma
//

// post.prisma

model Order {
  id        Int      @id @default(autoincrement())
  title     String   @db.VarChar(255)
  content   String   @db.VarChar()
  author    User?    @relation(fields: [authorId], references: [id])
  authorId  Int?
  create_at DateTime
  update_at DateTime

  @@map("orders") // chỉ định tạo ra bảng "posts"
}

//
// post.prisma
//

// post.prisma

model Post {
  id        Int      @id @default(autoincrement())
  title     String   @db.VarChar(255)
  content   String   @db.VarChar()
  author    User?    @relation(fields: [authorId], references: [id])
  authorId  Int?
  create_at DateTime
  update_at DateTime

  @@map("posts") // chỉ định tạo ra bảng "posts"
}

//
// user.prisma
//

// user.prisma

model User {
  id        Int       @id @default(autoincrement())
  email     String    @unique @db.VarChar(100)
  name      String?   @db.VarChar(50)
  password  String    @db.VarChar(100)
  status    Boolean   @default(true)
  create_at DateTime
  update_at DateTime
  Post      Post[]
  Courses   Courses[]
  Order     Order[]

  @@map("users") // chỉ định tạo ra bảng "users"
}
